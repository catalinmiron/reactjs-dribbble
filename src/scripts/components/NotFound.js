'use strict';

var React = require('react/addons');

require('../../styles/NotFound.less');

var NotFound = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for NotFound</p>
        </div>
      );
  }
});

module.exports = NotFound; 

